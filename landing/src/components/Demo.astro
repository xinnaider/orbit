<section class="product section" id="product" data-snap-section>
  <div class="product-head">
    <div>
      <p class="section-kicker" data-reveal>desktop dashboard</p>
      <h2 class="section-title" data-text-scrub>One window for every agent doing real work.</h2>
    </div>
    <p class="section-copy" data-reveal>
      Orbit turns parallel AI coding sessions into an inspectable operating surface: output streams,
      tool calls, diffs, token pressure, context windows, and spawned agents stay visible together.
    </p>
  </div>

  <div class="product-frame" data-reveal>
    <div class="frame-bar" aria-hidden="true">
      <span></span>
      <span></span>
      <span></span>
      <strong>orbit://project/agent-dashboard</strong>
    </div>
    <img src="/demo.gif" alt="Orbit showing multiple AI coding sessions in a desktop dashboard" loading="lazy" />
  </div>
</section>

<style>
  .product {
    padding-top: 92px;
  }

  .product-head {
    align-items: end;
    display: grid;
    gap: 40px;
    grid-template-columns: minmax(0, 1.05fr) minmax(300px, 0.72fr);
  }

  .product-frame {
    background: rgba(12, 13, 13, 0.78);
    border: 1px solid var(--line);
    border-radius: 8px;
    box-shadow: 0 42px 130px rgba(0, 0, 0, 0.52);
    margin-top: 54px;
    overflow: hidden;
  }

  .frame-bar {
    align-items: center;
    background: var(--bg2);
    border-bottom: 1px solid var(--line);
    display: flex;
    gap: 8px;
    height: 40px;
    padding: 0 14px;
  }

  .frame-bar span {
    border-radius: 999px;
    height: 9px;
    width: 9px;
  }

  .frame-bar span:nth-child(1) {
    background: var(--amber);
  }

  .frame-bar span:nth-child(2) {
    background: var(--green);
  }

  .frame-bar span:nth-child(3) {
    background: var(--cyan);
  }

  .frame-bar strong {
    color: var(--quiet);
    font-family: var(--mono);
    font-size: 12px;
    font-weight: 500;
    margin-left: 8px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .product-frame img {
    aspect-ratio: 16 / 9;
    background: rgba(244, 239, 228, 0.04);
    object-fit: cover;
    width: 100%;
  }

  @media (max-width: 860px) {
    .product-head {
      align-items: start;
      grid-template-columns: 1fr;
    }

    .product-frame {
      margin-top: 38px;
    }

  }

  @media (max-width: 520px) {
    .frame-bar strong {
      display: none;
    }
  }
</style>
