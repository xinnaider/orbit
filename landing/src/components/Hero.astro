<section class="hero" id="hero" data-snap-section>
  <div class="hero-inner">
    <div class="hero-copy-panel">
      <p class="hero-kicker">multi-provider desktop control room</p>
      <h1 class="hero-title">Orbit</h1>
      <p class="hero-copy">
        Run multiple AI coding agents in parallel, inspect their work in real time, and keep every
        session, token, cost, and spawned sub-agent under one desktop surface.
      </p>

      <div class="hero-actions">
        <a class="button button-primary" href="https://github.com/xinnaider/orbit/releases">
          <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
            <path d="m7 10 5 5 5-5"></path>
            <path d="M12 15V3"></path>
          </svg>
          Install Orbit
        </a>

        <a class="button button-secondary" href="https://github.com/xinnaider/orbit">
          <svg width="15" height="15" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
            <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0 0 16 8c0-4.42-3.58-8-8-8z"></path>
          </svg>
          View repo
        </a>

        <a class="button button-ghost" href="#product">
          See product
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M5 12h14"></path>
            <path d="m12 5 7 7-7 7"></path>
          </svg>
        </a>
      </div>

      <div class="install-panel hero-proof" data-install>
        <div class="install-head">
          <div>
            <strong>Install directly</strong>
            <span data-install-label>Windows selected</span>
          </div>
          <span class="install-live">ready</span>
        </div>

        <div class="os-grid" aria-label="Operating system">
          <button class="os-chip" type="button" data-os="win">Windows <span>10+</span></button>
          <button class="os-chip" type="button" data-os="mac">macOS <span>13+</span></button>
          <button class="os-chip" type="button" data-os="linux">Linux <span>AppImage</span></button>
        </div>

        <div class="install-command" aria-label="Install command">
          <span class="prompt" data-install-prompt>PS&gt;</span>
          <code data-install-command></code>
          <button class="copy-command" type="button" aria-label="Copy install command" data-copy-install>
            <svg class="copy-icon" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <rect x="9" y="9" width="13" height="13" rx="2"></rect>
              <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
            </svg>
            <svg class="done-icon" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="m20 6-11 11-5-5"></path>
            </svg>
          </button>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    align-items: center;
    display: flex;
    min-height: 100svh;
    overflow: hidden;
    padding: 128px 40px 74px;
    position: relative;
  }

  .hero-inner {
    display: grid;
    gap: clamp(28px, 4vw, 56px);
    grid-template-columns: minmax(0, 690px) minmax(260px, 390px);
    margin: 0 auto;
    max-width: 1160px;
    min-height: calc(100svh - 204px);
    position: relative;
    width: 100%;
    z-index: 1;
  }

  .hero-copy-panel {
    align-self: center;
    grid-column: 1;
    max-width: 690px;
  }

  .hero-kicker {
    color: var(--green);
    font-family: var(--mono);
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0;
    margin: 0 0 14px;
  }

  .hero-title {
    color: var(--text);
    font-family: var(--display);
    font-size: clamp(76px, 9vw, 136px);
    font-weight: 700;
    letter-spacing: 0;
    line-height: 0.82;
    margin: 0;
    text-shadow: 0 24px 80px rgba(0, 0, 0, 0.45);
  }

  .hero-copy {
    color: var(--muted);
    font-size: 17px;
    line-height: 1.48;
    margin: 28px 0 0;
    max-width: 680px;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 28px;
  }

  .button-ghost {
    background: transparent;
    color: var(--muted);
  }

  .button-ghost:hover {
    background: rgba(241, 236, 227, 0.05);
    border-color: var(--line-strong);
    color: var(--text);
  }

  .install-panel {
    backdrop-filter: blur(18px);
    background: rgba(12, 13, 13, 0.66);
    border: 1px solid var(--line);
    border-radius: 8px;
    box-shadow: 0 28px 90px rgba(0, 0, 0, 0.28);
    margin-top: 24px;
    max-width: 620px;
    overflow: hidden;
  }

  .install-head {
    align-items: center;
    border-bottom: 1px solid var(--line);
    display: flex;
    justify-content: space-between;
    padding: 14px 15px;
  }

  .install-head div {
    display: grid;
    gap: 2px;
  }

  .install-head strong {
    color: var(--text);
    font-size: 13px;
    font-weight: 700;
  }

  .install-head span,
  .install-live {
    color: var(--quiet);
    font-family: var(--mono);
    font-size: 11px;
  }

  .install-live {
    color: var(--green);
  }

  .os-grid {
    display: grid;
    gap: 8px;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    padding: 12px 14px 0;
  }

  .os-chip {
    background: rgba(241, 236, 227, 0.045);
    border: 1px solid var(--line);
    border-radius: 7px;
    color: var(--muted);
    cursor: pointer;
    display: grid;
    font-weight: 700;
    gap: 3px;
    min-height: 56px;
    padding: 10px 8px;
    place-items: center;
    transition:
      background 0.18s var(--ease),
      border-color 0.18s var(--ease),
      color 0.18s var(--ease);
  }

  .os-chip span {
    color: var(--quiet);
    font-family: var(--mono);
    font-size: 10px;
    font-weight: 500;
  }

  .os-chip:hover,
  .os-chip.is-active {
    background: var(--green-soft);
    border-color: rgba(124, 255, 158, 0.34);
    color: var(--green);
  }

  .install-command {
    align-items: center;
    background: rgba(0, 0, 0, 0.24);
    border: 1px solid var(--line);
    border-radius: 8px;
    display: flex;
    gap: 10px;
    margin: 12px 14px 14px;
    min-height: 48px;
    overflow: hidden;
    padding: 0 10px 0 15px;
  }

  .prompt {
    color: var(--green);
    flex: 0 0 auto;
    font-family: var(--mono);
    font-weight: 600;
  }

  .install-command code {
    color: var(--muted);
    flex: 1 1 auto;
    font-family: var(--mono);
    font-size: 12px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .copy-command {
    align-items: center;
    background: rgba(241, 236, 227, 0.04);
    border: 1px solid var(--line);
    border-radius: 8px;
    color: var(--quiet);
    cursor: pointer;
    display: flex;
    flex: 0 0 auto;
    height: 32px;
    justify-content: center;
    transition:
      border-color 0.18s var(--ease),
      color 0.18s var(--ease);
    width: 32px;
  }

  .copy-command:hover {
    border-color: rgba(143, 255, 178, 0.34);
    color: var(--green);
  }

  .done-icon {
    display: none;
  }

  .copy-command[data-state='copied'] .copy-icon {
    display: none;
  }

  .copy-command[data-state='copied'] .done-icon {
    display: block;
  }

  @media (max-width: 940px) {
    .hero {
      padding: 126px 24px 66px;
    }

    .hero-inner {
      gap: 34px;
      grid-template-columns: minmax(0, 1fr) minmax(120px, 0.28fr);
      min-height: calc(100svh - 192px);
    }
  }

  @media (max-width: 640px) {
    .hero {
      align-items: center;
      padding: 112px 18px 46px;
    }

    .hero-inner {
      display: block;
      min-height: 0;
    }

    .hero-copy-panel {
      margin-left: 0;
      max-width: none;
    }

    .hero-title {
      font-size: clamp(58px, 18vw, 84px);
    }

    .hero-copy {
      font-size: 14px;
      margin-top: 22px;
    }

    .hero-actions {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      margin-top: 24px;
    }

    .hero-actions .button {
      min-width: 0;
      padding: 0 12px;
      width: auto;
    }

    .hero-actions .button-ghost {
      grid-column: 1 / -1;
    }

    .install-command {
      align-items: flex-start;
      min-height: 0;
      padding: 12px;
    }

    .install-command code {
      white-space: normal;
      word-break: break-word;
    }
  }

  @media (max-width: 420px) {
    .os-chip {
      font-size: 12px;
      min-height: 50px;
      padding: 8px 4px;
    }

    .os-chip span {
      display: none;
    }
  }
</style>
